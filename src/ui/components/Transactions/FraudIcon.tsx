/* import React from "react";
import { Transaction } from "../../../domain/transaction/transaction";

type FraudIconProps = {
  onClick?: () => void;
  transaction: Transaction;
};

const FraudIcon = ({ onClick, transaction }: FraudIconProps) => {
  return (
    <img
      alt="fraud"
      onClick={onClick}
      src={transaction.isFraudulent ? "/warning-color.svg" : "/warning.svg"}
      className={"h-6 opacity-50 hover:opacity-100"}
    />
  );
};

export default FraudIcon;
 */
